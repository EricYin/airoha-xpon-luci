xpon-luci 2.3 2026-08-13
mode/status: 页面渲染彻底移除同步 xponigmpcmd 调用（ioctl 阻塞会挂起 LuCI→“登出”）；
新增 xpon-mvlan-snap.sh 后台刷新“驱动实际登记”快照 /tmp/xpon-mvlan-act.txt，页面只读快照；
provision 手动组播登记改后台 detach；status 页“IPTV 组播”诊断段改读快照
mode: 修复添加/删除仍登出——后台组播登记与 reboot 子 shell 补全 >/dev/null 2>&1 detach；
mode: vconfig/ip/ifconfig/gponmapcmd 全部 timeout 3 防 ioctl 挂死；
mode: 8021q 诊断改判 /proc/net/vlan（lsmod 查不到内建内核模块，不再误报“未加载”）；
mode: 删除接口恒连持久化段一起删，去掉“连持久化段”复选框；
mode: 组播新增“驱动实际登记”（xponigmpcmd mvlan show）回显
vlan: ctl_ver=2 (manual GEM write)
status: optical DDM (bob_info) + PON traffic bars
auth: LOID default note
status: CPU temp via /usr/bin/xpon-cputemp.sh (thermal_zone -> cputemp_cmd -> devmem ThermalGetT_7581)
auth: read-back runtime values (omcicfgCmd get + PON MAC + onu_type + sys_xpon_mode)
status: DDM cards Vcc/Ibias/Imod
vlan: no-OMCI (pure manual) guidance box
cputemp: static aarch64 /usr/bin/xpon-cputemp (mmap /dev/mem, no busybox devmem needed)
auth: 认证方式仅 LOID/PASSWORD；移动 Password 独立 REG_ID（无需 SN 密码）；各字段回显当前生效值
mode: PON VLAN 接口管理（pon.<VID> 添加/删除 + xpon_vlan 持久化）
vlan: 页面改名 OLT 下发情况
status: 去 LOID 卡片；onu_type 显示 SFU/HGU 形态；Vcc DDM 读取加 dmesg 全量回退；流量长条加高
mode: 删技术详情；PON VLAN 区块读回 wan_vlan 持久化配置（修正 section 类型 wan_vlan）
mode: ONU 形态下拉精简为 2 项（HGU/SFU），注释精简
status: PON 接口流量独占一行（RX/TX 长条并排）
vlan: 菜单/标题改名回「下发情况」
mode: PON VLAN 列表多格式读取（proc/ip/ifconfig）+ 保存 pcall 保护 + 优先 vconfig
mode: 删除顶部大段说明
UI: 全页面说明精简（auth/mode/vlan/status）
mode: PON VLAN 添加/删除改 GET+token（绕开 LuCI 21.02 POST 会话拦截，修复“点添加被登出”）
mode: PON VLAN 读取加 sysfs /sys/class/net 兜底 + 空表内联诊断原始输出
mode: 修复删除仍登出——persisted 表删除表单与 ONU 形态表单全部改 GET+token（mode.htm 残留 POST 已清零）
mode: 组播登记改后台异步（xponigmpcmd 同步会挂起 LuCI 请求）；接口/持久化表新增组播 M-VLAN 列回显
